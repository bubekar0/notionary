# This file exists only so Git tracks the otherwise-empty cache/ directory.
# The app writes its JSON response cache here at runtime; the directory must
# exist and be writable. Cache files themselves are git-ignored.
